import { branding } from './config/branding.js?v=20260819-3';
import { icon } from './ui.js?v=20260819-3';
import * as screens from './screens.js?v=20260819-3';
const tg=window.Telegram?.WebApp;tg?.ready();tg?.expand();
const supportsBack=Boolean(tg?.isVersionAtLeast?.('6.1'));
const supportsHaptics=Boolean(tg?.isVersionAtLeast?.('6.1'));
const header=document.querySelector('#header'),content=document.querySelector('#content'),bottomNav=document.querySelector('#bottomNav'),toast=document.querySelector('#toast');
const state={view:'home',account:null,loading:true,form:JSON.parse(sessionStorage.getItem('agent-form')||'{}')};
const nav=[['home','Главная','home'],['stats','Статистика','chart'],['profile','Кабинет','user']];
const api=(path,options={})=>fetch(path,{...options,headers:{'Content-Type':'application/json','X-Telegram-Init-Data':tg?.initData||'',...(options.headers||{})}});
function note(text){toast.textContent=text;toast.classList.add('show');clearTimeout(note.t);note.t=setTimeout(()=>toast.classList.remove('show'),2600)}
function headerView(){header.innerHTML=`<button class="portal-mark" data-nav="home">MB</button><div class="portal-title"><b><span class="brand-mel">MEL</span><span class="brand-bet">BET</span> PARTNERS</b><span>AGENTS & PARTNERS</span></div><button class="header-avatar" data-nav="profile">${(state.account?.telegram?.first_name||'P')[0]}</button>`}
function render(){const routes={home:()=>screens.home(state.account),stats:()=>screens.stats(state.account),profile:()=>screens.profile(state.account),agents:()=>screens.agents(state.account),partners:screens.partners,banners:screens.banners,contact:()=>screens.lookup(false),blocked:()=>screens.lookup(true),faq:screens.faq,apply:()=>screens.application(state.form),check:()=>screens.check(state.account),instructions:screens.instructions,support:screens.support,ticket:screens.ticket};headerView();content.innerHTML=state.loading?screens.loading():(routes[state.view]||routes.home)();bottomNav.innerHTML=nav.map(([v,l,i])=>`<button class="nav-item ${state.view===v?'active':''}" data-nav="${v}">${icon(i)}<span>${l}</span></button>`).join('');if(supportsBack)state.view==='home'?tg.BackButton.hide():tg.BackButton.show()}
function go(view){state.view=view;if(supportsHaptics)tg.HapticFeedback.impactOccurred('light');render();scrollTo({top:0,behavior:'smooth'})}
async function load(){try{const r=await api('/api/me');if(r.ok)state.account=await r.json()}catch{}finally{state.loading=false;render()}}
document.addEventListener('click',e=>{const target=e.target.closest('[data-nav]');if(target)return go(target.dataset.nav);const faq=e.target.closest('.faq-question');if(faq)faq.parentElement.classList.toggle('open')});
document.addEventListener('input',e=>{if(e.target.closest('[data-form="application"]')){state.form={...state.form,[e.target.name]:e.target.value};sessionStorage.setItem('agent-form',JSON.stringify(state.form))}});
document.addEventListener('submit',async e=>{const form=e.target,type=form.dataset.form;if(!type)return;e.preventDefault();const button=form.querySelector('[type="submit"]');button.disabled=true;const data=Object.fromEntries(new FormData(form));try{if(type==='lookup'){const blocked=form.dataset.check==='blocked';const r=await api(blocked?'/api/check-blocked':'/api/check-contact',{method:'POST',body:JSON.stringify({query:data.query})});if(!r.ok)throw Error();const result=await r.json(),box=form.parentElement.querySelector('.lookup-result');box.hidden=false;box.innerHTML=blocked?(result.blocked?'⛔ Контакт заблокирован':'✅ Контакт не находится в блок-листе'):(result.registered?'⚠️ Контакт уже есть в системе':'✅ Контакт свободен');return}const endpoint=type==='application'?'/api/agent-applications':'/api/support-tickets';const body=type==='application'?data:{subject:data.subject,category:data.category,body:data.body};const r=await api(endpoint,{method:'POST',body:JSON.stringify(body)});if(!r.ok)throw Error();if(type==='application'){sessionStorage.removeItem('agent-form');state.form={};await load();note('Заявка отправлена');go('home')}else{note('Обращение отправлено');go('support')}}catch{note('Не удалось выполнить действие. Откройте приложение в Telegram и попробуйте снова.')}finally{button.disabled=false}});
if(supportsBack)tg.BackButton.onClick(()=>go('home'));load();
